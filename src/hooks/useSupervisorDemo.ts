import { useEffect } from "react";

export const useSupervisorDemo = () => {
  // Hook vazio - agora as respostas do Gemini são adicionadas automaticamente
  // através do SupervisorSummaryButtons quando lastAIResponse é recebida
  useEffect(() => {
    // Não há dados de demonstração - apenas respostas reais do Gemini
  }, []);
};
