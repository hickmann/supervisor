[package]
name = "coterapia"
version = "0.1.1"
description = "Um copiloto de supervisão para psicólogos: analisa sessões em tempo real, sugere insights clínicos, organiza tarefas e apoia decisões com base em evidências, de forma ética e segura."
authors = ["CoterapIA <suporte@coterapia.com.br>"]
license = "Copyright CoterapIA 2025"
homepage = "https://www.coterapia.com.br"
readme = "../README.md"
keywords = [
  "copiloto-psicologo",
  "supervisao-clinica",
  "ai-assistant",
  "psicologia-evidencias",
  "assistente-terapeutico",
  "analise-sessoes",
  "terapia-online",
  "desktop-app",
  "privacy-first",
  "apoio-clinico"
]
categories = [
  "psychology",
  "ai-tools",
  "desktop-app",
  "privacy-security",
  "productivity"
]
edition = "2021"

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "coterapia_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }
dotenv = "0.15"

[dependencies]
tauri = { version = "2", features = ["macos-private-api"] }
tauri-plugin-opener = "2.5.0"
tauri-plugin-updater = "2.9.0"
tauri-plugin-http = "2.5.2"
tauri-plugin-global-shortcut = "2"
tauri-plugin-keychain = "2.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
image = "0.25.6"
xcap = "0.0.12"
base64 = "0.22"
cpal = "0.15.3"
hound = "3.5.1"
tokio = { version = "1.0", features = ["full"] }
once_cell = "1.19.0"
uuid = { version = "1.0", features = ["v4"] }
reqwest = { version = "0.12", features = ["json", "stream"] }
dotenv = "0.15"
futures-util = "0.3"
anyhow = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "ansi", "json"] }
dirs = "5.0"
ringbuf = "0.4.8"
tauri-plugin-shell = "2.3.1"
tempfile = "3.8"

[target.'cfg(target_os = "macos")'.dependencies]
tauri-plugin-macos-permissions = "2"
cidre = "0.11.3"

[target.'cfg(target_os = "windows")'.dependencies]
wasapi = "0.19.0"

[target.'cfg(target_os = "linux")'.dependencies]
libpulse-binding = "2.30.1"
libpulse-simple-binding = "2.29.0"
